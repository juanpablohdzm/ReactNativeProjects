class Place {
    constructor(title, imageUri, location){
     this.title = title;
        this.imageUri = imageUri,
        thiis.address = address,
        this.location = location,
        this.id =  new Date().toString() + Math.random().toString();
    }
}